package atomspace.query.basic;

import atomspace.query.QueryEngine;
import atomspace.storage.ASAtom;

import java.util.Iterator;

public class BasicQueryEngine implements QueryEngine {

    @Override
    public Iterator<ASAtom> query(ASAtom atom) {
        return null;
    }
}
